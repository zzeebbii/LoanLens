node_modules/
dist/
coverage/
*.local
.DS_Store
.env
.env.*
!.env.example
.vite/
*.tsbuildinfo

# Loan exports. These are the user's own financial records, not project files, and they land
# in the repository root by default — one `git add -A` away from being published.
loanlens-backup-*.json
